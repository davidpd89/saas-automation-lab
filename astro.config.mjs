import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://davidpd89.github.io",
  base: "/temp-saas-ai-affiliate-site",
  output: "static",
  trailingSlash: "always"
});
