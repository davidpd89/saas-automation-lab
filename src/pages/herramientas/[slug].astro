---
import { getCollection } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";
import AffiliateButton from "../../components/AffiliateButton.astro";

export async function getStaticPaths() {
  const items = await getCollection("tools");
  return items.map((item) => ({
    params: { slug: item.slug },
    props: { item }
  }));
}

const { item } = Astro.props;
const { Content } = await item.render();
const siteUrl = new URL(import.meta.env.BASE_URL, Astro.site ?? new URL("https://davidpd89.github.io")).toString();
const faqItems = [
  {
    question: `Para quien es ${item.data.title}?`,
    answer: item.data.description
  },
  {
    question: `Cuanto cuesta ${item.data.title}?`,
    answer: `El precio de referencia es ${item.data.pricing}. Verifica siempre la pagina oficial antes de contratar.`
  },
  {
    question: "Esta recomendacion incluye enlaces de afiliado?",
    answer: "Si. Algunos enlaces pueden generar una comision sin coste adicional para el lector."
  }
];

// Schema markup for tool pages
const toolSchema = {
  "@context": "https://schema.org",
  "@type": "Review",
  "itemReviewed": {
    "@type": "SoftwareApplication",
    "name": item.data.title,
    "applicationCategory": "BusinessApplication",
    "operatingSystem": "Web Browser",
    "offers": {
      "@type": "Offer",
      "price": item.data.pricing,
      "priceCurrency": "EUR"
    }
  },
  "reviewRating": {
    "@type": "Rating",
    "ratingValue": item.data.rating,
    "bestRating": 10,
    "worstRating": 1
  },
  "author": {
    "@type": "Person",
    "name": "David Porto",
    "url": "https://github.com/davidpd89"
  },
  "publisher": {
    "@type": "Organization",
    "name": "Automatización SaaS e IA",
    "url": siteUrl
  }
};
const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": faqItems.map((faq) => ({
    "@type": "Question",
    "name": faq.question,
    "acceptedAnswer": {
      "@type": "Answer",
      "text": faq.answer
    }
  }))
};
---

<BaseLayout title={item.data.seoTitle} description={item.data.seoDescription}>
  <script type="application/ld+json" set:html={JSON.stringify(toolSchema)} />
  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
  
  <article class="max-w-4xl mx-auto px-4 py-8">
    <header class="mb-8">
      <h1 class="text-4xl font-bold text-gray-900 mb-4">
        {item.data.title}
      </h1>
      
      <div class="flex flex-wrap items-center gap-4 mb-6 text-sm text-gray-600">
        <span class="bg-blue-100 text-blue-800 px-3 py-1 rounded-full">
          {item.data.idealUser}
        </span>
        <span class="bg-green-100 text-green-800 px-3 py-1 rounded-full">
          {item.data.pricing}
        </span>
        <span class="flex items-center">
          ⭐ {item.data.rating}/10
        </span>
      </div>
      
      <p class="text-xl text-gray-700 leading-relaxed">
        {item.data.description}
      </p>
    </header>

    <section class="mb-12">
      <div class="bg-blue-50 border border-blue-200 rounded-lg p-6">
        <h2 class="text-lg font-semibold text-blue-900 mb-4">Para quién es esta herramienta</h2>
        <p class="text-blue-800">
          Ideal para <strong>{item.data.idealUser}</strong> que necesita automatizar procesos y escalar su negocio sin aumentar el tiempo manual.
        </p>
        
        <div class="mt-6">
          <AffiliateButton 
            url={item.data.affiliateUrl} 
            label="Probar gratis" 
            toolId={item.data.toolId}
            variant="primary"
            size="lg"
            showDisclosure={true}
          />
        </div>
      </div>
    </section>

    <section class="prose prose-lg max-w-none">
      <Content />
    </section>

    <section class="mt-12 p-6 bg-gray-50 rounded-lg border">
      <p class="eyebrow">E-E-A-T</p>
      <h2>Como evaluamos esta herramienta</h2>
      <p>
        Revisamos encaje por perfil, claridad de precios, integraciones, curva de aprendizaje,
        transparencia del programa de afiliados y utilidad real para solopreneurs de servicios.
      </p>
    </section>

    {item.data.affiliate && (
      <section class="mt-12 p-6 bg-gray-50 rounded-lg">
        <h2 class="text-lg font-semibold text-gray-900 mb-4">Información de Afiliación</h2>
        <div class="grid md:grid-cols-2 gap-4 text-sm">
          <div>
            <strong>Comisión:</strong> {item.data.affiliate.commissionRange}
          </div>
          <div>
            <strong>Tipo:</strong> {item.data.affiliate.commissionType}
          </div>
          <div>
            <strong>Cookie:</strong> {item.data.affiliate.cookieDays} días
          </div>
          <div>
            <strong>Plataforma:</strong> {item.data.affiliate.platform}
          </div>
        </div>
      </section>
    )}

    <section class="mt-12 p-6 bg-white rounded-lg border">
      <h2>Preguntas frecuentes</h2>
      {faqItems.map((faq) => (
        <details>
          <summary>{faq.question}</summary>
          <p>{faq.answer}</p>
        </details>
      ))}
    </section>
  </article>
</BaseLayout>
